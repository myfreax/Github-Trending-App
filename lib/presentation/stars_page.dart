import 'package:flutter/material.dart';

class StarsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('stars view'),
      ),
    );
  }
}
